package com.bugisiw.marketplace.common.model.billing;

/**
 * Enum representing the different pricing models available for agents.
 */
public enum PricingModel {
    FREE,           // Free tier with limited features
    SUBSCRIPTION,   // Subscription-based pricing
    PAY_PER_REQUEST,// Pay per request/usage
    CREDITS         // Credit-based system
} 