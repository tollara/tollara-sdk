{
  "id": "agentvend",
  "name": "AgentVend",
  "version": "1.0.0",
  "description": "Invoke agents on AgentVend (caller) or act as backend with HMAC verification and usage reporting",
  "configSchema": {
    "type": "object",
    "properties": {
      "mode": {
        "type": "string",
        "enum": ["caller", "backend"],
        "default": "caller",
        "description": "caller = invoke agents; backend = receive gateway requests, verify HMAC, report usage"
      },
      "gatewayUrl": {
        "type": "string",
        "description": "Gateway base URL (caller mode)"
      },
      "agentKey": {
        "type": "string",
        "description": "Agent key for invoke (caller mode)"
      },
      "apiUrl": {
        "type": "string",
        "description": "AgentVend API origin (optional; defaults to https://api.agentvend.api). Used when reporting usage in backend mode."
      },
      "agentSecret": {
        "type": "string",
        "description": "Agent secret for HMAC (backend mode)"
      }
    }
  },
  "skillPaths": ["skills/agentvend"]
}
