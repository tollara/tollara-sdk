{
  "id": "tollara",
  "name": "Tollara",
  "version": "1.0.0",
  "description": "Invoke services on Tollara (caller) or act as backend with HMAC verification and usage reporting",
  "configSchema": {
    "type": "object",
    "properties": {
      "mode": {
        "type": "string",
        "enum": ["caller", "backend"],
        "default": "caller",
        "description": "caller = invoke services; backend = receive gateway requests, verify HMAC, report usage"
      },
      "gatewayUrl": {
        "type": "string",
        "description": "Gateway base URL (caller mode)"
      },
      "serviceKey": {
        "type": "string",
        "description": "Service key for invoke (caller mode)"
      },
      "apiUrl": {
        "type": "string",
        "description": "Tollara API origin (optional; defaults to https://api.tollara.ai). Used when reporting usage in backend mode."
      },
      "serviceSecret": {
        "type": "string",
        "description": "Service secret for HMAC (backend mode)"
      }
    }
  },
  "skillPaths": ["skills/tollara"]
}
