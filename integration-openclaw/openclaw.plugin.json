{
  "id": "agentvend",
  "name": "AgentVend",
  "version": "1.0.0",
  "description": "Invoke services on AgentVend (caller) or act as backend with HMAC verification and usage reporting",
  "configSchema": {
    "type": "object",
    "properties": {
      "mode": {
        "type": "string",
        "enum": ["caller", "backend"],
        "default": "caller",
        "description": "caller = invoke services; backend = receive gateway requests, verify HMAC, report usage"
      },
      "gatewayUrl": {
        "type": "string",
        "description": "Gateway base URL (caller mode)"
      },
      "serviceKey": {
        "type": "string",
        "description": "Service key for invoke (caller mode)"
      },
      "apiUrl": {
        "type": "string",
        "description": "AgentVend API origin (optional; defaults to https://api.agentvend.api). Used when reporting usage in backend mode."
      },
      "serviceSecret": {
        "type": "string",
        "description": "Service secret for HMAC (backend mode)"
      }
    }
  },
  "skillPaths": ["skills/agentvend"]
}
