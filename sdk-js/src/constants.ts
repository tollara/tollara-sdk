/** Default Tollara API origin (production). */
export const DEFAULT_API_URL = 'https://api.tollara.ai';

/** Path segments joined to `baseUrl` for each service (Docker/local default). */
export const DEFAULT_CORE_PATH_PREFIX = '/api/v1';
export const DEFAULT_GATEWAY_PATH_PREFIX = '/api';
export const DEFAULT_USAGE_PATH_PREFIX = '/api/usage';

/** ECS / hosted api.tollara.ai path prefixes (see MAIN-SDK-API-SPEC.md). */
export const ECS_CORE_PATH_PREFIX = '/core/api/v1';
export const ECS_GATEWAY_PATH_PREFIX = '/gateway/api/v1';
export const ECS_USAGE_PATH_PREFIX = '/usage/api/v1';
